# macOS runtime deps for the Switcher AC backend (server + control loop).
# (aioswitcher is vendored under ./src and loaded via PYTHONPATH=src.)
#
# Install into the repo venv:
#   .venv/bin/pip install -r requirements-mac.txt
#
# The menu-bar widget is a separate native SwiftUI app under macapp/ — it has
# no Python deps (built with `swift build`, talks to the server over HTTP).
fastapi==0.128.8
uvicorn==0.39.0
python-dotenv==1.2.1
